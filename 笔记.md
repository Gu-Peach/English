# monorepo+微服务的搭建

## vue中路由注册的方式

### 1. App.vue 中两者的作用

```vue
`
<template>
  <RouterView />
</template>
<script setup lang="ts">
import { RouterView } from "vue-router";
</script>
`
```

- `import { RouterView }` ：只是把 vue-router 包里的这个组件引入进来，让`<script setup>` 能在模板中使用它。严格说这行导入是 可选的 ——因为后面`app.use(router)` 时 vue-router 会把`RouterView` /`RouterLink` 注册成 全局组件 ，不导入也能写`<RouterView />` 。显式导入只是让来源更清晰。
- `<RouterView />` ：顶层路由出口。它做的事情是：从注入的 router 实例里读取"当前匹配的路由"，然后渲染匹配到的组件。它就是整个组件树的"第 1 层坑"。
  所以它不是"把路由提供给 App.vue"，而是 反过来 ：App.vue 从 router 那里取出当前路由，渲染对应页面。

### 2. 路由是什么时候注册进去的？

三个阶段，对应三个文件：

阶段一：路由表在模块加载时就被"捕获"

router/index.ts 第 4-10 行：

```ts
`const router = createRouter({
  history: createWebHistory(...),
  routes: [...home, ...wordBook],   // 路由表在这里传入
})`;
```

`createRouter()` 执行的那一刻（这个文件被 import 时），路由表就已经存进 router 实例内部了。此时只是"有了字典"，还没和 Vue 应用发生关系。

阶段二：`app.use(router)` 把 router 装进应用

main.ts 第 27 行—— 这才是真正的"注册"时机 。`app.use(router)` 会调用 vue-router 的`install()` 方法，它做了四件事：

1. 把 router 实例注入(provide)到整个组件树 → 之后任何组件里的`<RouterView>` 、`useRouter()` 、`useRoute()` 都从这里拿
2. 全局注册`RouterView` 和`RouterLink` 组件
3. 创建响应式的`currentRoute` 状态
4. 监听浏览器地址变化（popstate），驱动视图更新
   阶段三：`app.mount('#app')` 渲染第一帧

挂载后 App.vue 开始渲染，它里面的`<RouterView />` 从注入的 router 读取当前 URL 匹配到的路由（比如`/` ），渲染出 layout，layout 里 Content 的第二个`<RouterView />` 再渲染子路由 Home。

### 3.`<RouterView />` 是出口

`<RouterView>` 内部有一个"当前深度"的概念：

- App.vue 里的 = 深度 0 → 渲染路由表 第一层 匹配到的组件（即`component: layout` ）
- Content 里的 = 深度 1 → 渲染 第二层 （即`children` 里的 Home / WordBook）

## 完整对照

以访问`/word-book/index` 为例，路由匹配结果是：

```text
`matched: [
  0 → { path: '/word-book', component: layout }      ← 父
  1 → { path: 'index', component: WordBook }         ← 子
]`
```

渲染链：

```text
`App.vue
  <RouterView />          ← 消费 matched[0]，渲染 layout
    └─ layout/index.vue
        ├─ <Header />      ← 普通组件，与路由无关
        └─ <Content />
            <RouterView /> ← 消费 matched[1]，渲染 WordBook`
```

当点击触发路由切换的时候，会改变router的状态，所有 RouterView 各自响应状态变化。

## 实际发生的顺序

点击"词库"后：

```text
`1. router.push('/word-book/index')
      ↓
2. router 内部：history.pushState 改地址栏 URL
      ↓
3. router 重新匹配路由表，更新响应式状态 currentRoute
   （matched: [layout, WordBook]）
      ↓
4. 所有依赖这个状态的组件自动重渲染 ← RouterView 就是其中之一`
```

但要注意一点：切换路由时不是"只影响当前深度"，而是每一层的 RouterView 都会重新求值，只是求值结果变不变决定了要不要重建。

## 机制细节

路由切换时，router 的`currentRoute` 是响应式的， 所有层级的 RouterView 都会被触发重新渲染 。之后走 Vue 的 diff：

```text

`从 / 切到 /word-book/index：

App.vue 的 RouterView（深度 0）
  渲染前：matched[0] 的 component = layout
  渲染后：matched[0] 的 component = layout   ← 同一个组件
  → Vue diff 发现 vnode 类型相同 → 复用实例，不卸载不重建

Content 的 RouterView（深度 1）
  渲染前：matched[1] = Home
  渲染后：matched[1] = WordBook              ← 组件变了
  → 卸载 Home，挂载 WordBook`
```

所以外壳不刷新的根本原因是： 两条路由在深度 0 上解析出的是同一个组件 ，Vue 直接复用实例。
